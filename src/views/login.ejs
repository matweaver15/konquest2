<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Log In</title>
    <style>
      body { font-family: system-ui, sans-serif; margin: 0; padding: 2rem; background: #f4f6fb; }
      main { max-width: 420px; margin: 0 auto; background: #fff; padding: 2rem; border-radius: 12px; box-shadow: 0 10px 30px rgba(36, 42, 65, 0.12); }
      h1 { margin-top: 0; font-size: 1.8rem; }
      form { display: grid; gap: 1rem; }
      label { display: grid; gap: 0.5rem; font-weight: 600; }
      input { padding: 0.75rem; border: 1px solid #cfd6e6; border-radius: 8px; font-size: 1rem; }
      button { padding: 0.75rem 1rem; border: none; border-radius: 8px; background: #2d5bff; color: #fff; font-size: 1rem; cursor: pointer; }
      button:hover { background: #244ad6; }
      .errors { margin: 0 0 1rem; padding: 0.75rem 1rem; border-radius: 8px; background: #ffecef; color: #7b001c; list-style: none; }
      .errors li { margin: 0.25rem 0; }
      .switch { margin-top: 1rem; font-size: 0.95rem; }
      a { color: #2d5bff; text-decoration: none; }
      a:hover { text-decoration: underline; }
    </style>
  </head>
  <body>
    <main>
      <h1>Log In</h1>
      <% if (errors.length) { %>
        <ul class="errors">
          <% errors.forEach((error) => { %>
            <li><%= error.msg %></li>
          <% }) %>
        </ul>
      <% } %>
      <form method="post" action="/auth/login" novalidate>
        <label>
          Username
          <input
            type="text"
            name="username"
            value="<%= values.username %>"
            autocomplete="username"
            required
            minlength="3"
            maxlength="20"
          />
        </label>
        <label>
          Password
          <input type="password" name="password" autocomplete="current-password" required />
        </label>
        <button type="submit">Log in</button>
      </form>
      <p class="switch">Need an account? <a href="/auth/register">Create one</a>.</p>
    </main>
  </body>
</html>
